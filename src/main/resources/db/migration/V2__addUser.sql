CREATE TABLE AppUser(
    id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL CONSTRAINT pk_user PRIMARY KEY,
    name TEXT NOT NULL,
    password_hash TEXT NOT NULL,
    email TEXT NOT NULL
        CONSTRAINT uq_user_email UNIQUE,
    accessId INTEGER NOT NULL
);